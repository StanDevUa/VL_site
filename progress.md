# Прогрес розробки — Ресурс 1 (сайт-візитка Вікторії Лемешко)

Робочий журнал: що вже зроблено, які рішення були прийняті і чому, що далі. Оновлюється в кінці кожної сесії. Деталі архітектурних рішень — в `architecture.md`, вимоги — в `tech_specs.md`.

## Зроблено

### Інфраструктура
- Next.js (App Router) + TypeScript + Prisma 6.19.3 (навмисно не Prisma 7 — там прибрали `datasource.url`, ламає поточний підхід) + PostgreSQL.
- Auth.js v5, сесії через **JWT** (не database — Credentials-провайдер це вимагає).
- next-intl (uk за замовчуванням, en/ru); в адмінці локалізація вимкнена — тільки українська.
- Файлове сховище — self-hosted MinIO (S3-сумісне) на сервері Стаса. Це фінальне рішення й для продакшена, не тільки для розробки. В БД зберігаються відносні ключі, а не повні URL (`getPublicUrl()`), щоб можна було міняти розташування сховища без міграції даних.
- Tailwind v4, дизайн-токени в `globals.css` (`@theme`), кольори з дизайн-системи (navy/coral/magenta/violet/blue/indigo/powder).

### Адмінка + публічні сторінки (vertical slices)
Готові повністю (CRUD в адмінці + відповідні публічні сторінки):
- **Мої роботи** (портфоліо)
- **Новини та анонси** — з відкладеною публікацією (без нового поля в БД: існуюче поле `date` використовується як фільтр видимості, `publishedNewsWhere()`)
- **Відгуки**
- **Дипломи** — з перемикачем «Показувати на сайті» (`showOnSite`)
- **FAQ** — з перемикачем «Показувати на головній сторінці» (`showOnHome`)

Готові в адмінці:
- **Категорії** — назва (3 мови) + слаг; картки в списку, 5 в ряд, з мініатюрою першого товару категорії + датою створення; видалення заблоковане, поки в категорії є товари
- **Товари** — назва/тип/склад/опис (3 мови кожне), категорія, ціна, головне фото (обов'язкове), галерея (необов'язково), перемикач «Показувати на головній»; картки 4 в ряд (8 на сторінку — 2 ряди, влазить на екран без скролу), фільтр за категорією + «Знайдено: N»; форма блокує створення товару, поки немає жодної категорії

Готові публічні сторінки магазину (`/shop`, `/shop/[slug]`, `/cart`, `/checkout`, `/checkout/pay`, `/checkout/success`):
- **Каталог** — фільтр за категоріями (динамічний, з БД), сітка 4 в ряд, картка товару з градієнтним бейджем категорії поверх фото (точна специфікація з макета), пагінація
- **Картка товару** — галерея з клікабельними мініатюрами, опис, Тип товару/Характеристики, ціна + лічильник «У кошику» + степер кількості, «Рекомендовані товари» (та ж категорія)
- **Кошик** — **поки повністю на localStorage** (не БД): свого логіну покупця до чекауту не існує (акаунт створюється лише під час оформлення замовлення), тому "БД-кошик, прив'язаний до сесії" з `architecture.md` розд. 5.2 поки не застосовний — узгоджено з користувачем. `CartProvider` на `useSyncExternalStore` (не `useState`+`useEffect` — уникає гідратаційного розсинхрону І `react-hooks/set-state-in-effect`)
- **Чекаут** — форма контактів + доставки + резюме замовлення повністю робоча, `createOrder` створює `User` (за email, +роль `PURCHASER`) і `Order`/`OrderItem` в БД зі статусом `PENDING_PAYMENT`.
  - **Місто/відділення Нової пошти — живий пошук через API** (`NovaPoshtaFields`, проксі `/api/nova-poshta/{cities,warehouses}`, ключ `NOVA_POSHTA_API_KEY` в `.env`, тільки на сервері) — підключено 2026-09-24; довідникове API (getCities/getWarehouses) не вимагає підписаного договору, на відміну від реального створення накладних.
  - **WayForPay підключений на тестовому мерчанті** (`test_merch_n1` — публічний демо-акаунт із офіційної документації WayForPay, не приватний секрет) — підключено й **перевірено 2026-09-24**: створив тестове замовлення в БД, `/checkout/pay` коректно згенерував форму з вірним підписом HMAC_MD5 (`src/lib/wayforpay.ts`); змоделював реальний вебхук-запит із правильною підписом на `/api/payments/wayforpay/callback` — сервер прийняв, відповів коректним ack, і замовлення реально перейшло в `PAID` (з `paidAt`/`wayforpayStatus`) у БД; `/checkout/success` показав правильний статус. Не перевірявся лише сам живий редирект на сторінку WayForPay й повернення звідти (це вже їхня сторона — вимагає публічної адреси, деплой свідомо відкладено). Коли буде реальний договір — заміна лише значень у `.env` (`WAYFORPAY_MERCHANT_ACCOUNT`/`_SECRET_KEY`/`_DOMAIN`), код чіпати не треба.

Публічний сайт поки без header/footer/навігації — це окрема майбутня фаза "сам сайт" (за словами Стаса), тому на ці сторінки можна потрапити лише напряму по URL.

Готова адмінка **Замовлення** (`/admin/zamovlennia`):
- Список: комбіновані фільтри (не таби — вони мають діяти одночасно, а не перемикати вид) — Період від/до, Статус, Категорія, Товар, через звичайну GET-форму (без JS); «Знайдено: N»; номер + бейдж статусу + клієнт + дата + сума; пагінація зберігає активні фільтри
- Картка замовлення: товари з сумою по кожному, контакти, реальні дані доставки з Нової пошти
- Зміна статусу — **ручні кнопки** (Позначити оплаченим → Позначити відправленим, Скасувати), а не вільний select. «Оплачено» тепер МОЖЕ проставлятись і автоматично через WayForPay-вебхук (коли той запрацює наживо) — ручна кнопка лишається як fallback/для готівки поза системою, кнопки для «Відправлено» так і залишаться ручними завжди (Вікторія відправляє фізично сама)

Усі форми адмінки побудовані на єдиному, вже відпрацьованому паттерні:
- `useActionState` + серверні екшени, що повертають `{ fieldErrors, values }`
- Живе зникнення помилки поля при заповненні
- `LocaleTabs` для трьох мовних версій тексту
- `PhotoPicker` для обов'язкового фото (уся головна фотографія — завжди обов'язкова, це принципове рішення)
- Кастомна серверна валідація замість браузерних popup (`noValidate`)

### UX-полірування адмінки
- Уніфіковані кольори кнопок: `primaryButtonClass` (індиго) / `secondaryButtonClass` (magenta hover — це підтверджено як єдиний правильний hover-колір по всіх реальних макетах, не indigo)
- Курсор-«рука» на всіх кнопках і посиланнях глобально
- Приховані скролбари всюди, окрім `textarea`
- Бейдж **«Приховано»** (сірий, `text-navy-soft bg-navy/5`) додано в списки Відгуків і FAQ — тепер однаково з Дипломами, візуально відрізняється від «На головній» (indigo)
- Підсвітка активного пункту меню в сайдбарі (лише magenta текст, без фону) — винесено в окремий client-компонент `AdminSidebarNav`, бо потрібен `usePathname()`. «Магазин» (розкривний пункт з підпунктами Категорії/Товари) підсвічується строго по маршруту, так само як і решта пунктів — розгортання/згортання суто механічне (стрілка), без власного кольорового стану, інакше він конфліктував з підсвіткою реальної поточної сторінки
- Категорійний градієнтний бейдж (`linear-gradient(120deg, #F2662F 0%, #C9307C 42%, #7A3AA0 70%, #2B6BB8 100%)`, білий uppercase-текст) — точна специфікація з `design/VL - Shop (client).html`, використовується на картках категорій поверх мініатюри товару

## Важливі особливості / підводні камені

- **Windows EPERM при `prisma generate`**: якщо в цей момент запущений `npm run dev`, він тримає відкритим `query_engine-windows.dll.node`, і Prisma Client не перегенеровується повністю. Міграції БД завжди проходять коректно через SQL, але сам JS-клієнт треба регенерувати вручну — Стасу потрібно повністю зупинити dev-сервер і перезапустити. Один раз це реально спричинило видиму помилку (поле `showOnSite` показувало не те значення, поки не перезапустили).
- **Дисципліна по обсягу змін**: правити рівно те, що попросили; питати перед будь-яким дизайн/UX-рішенням або розширенням обсягу. Функціональні наслідки вже узгодженого рішення — уточнювати не треба, можна одразу робити.
- Файлові інпути не можна відновити через `defaultValue` — тільки зберігати сам `File` в state і повертати через `DataTransfer`.
- Радіобатони й інші контрольовані інпути після Server Action React може «не домалювати» в DOM — рішення: `defaultChecked`/`defaultValue` (uncontrolled) замість `checked`/`value`.
- **`<button>` всередині `<form>` у флекс-рядку (напр. «Редагувати посилання + Видалити кнопка») може візуально «не стояти на одній лінії» з сусіднім `<a>`**, навіть якщо в button скинуті всі padding/border/font і computed-стилі співпадають один-в-один. Причина — `<form>` (або будь-яка проміжна block-обгортка) є справжнім флекс-елементом, а не сама кнопка всередині неї; блочна обгортка має власний line-box "струт", прив'язаний до успадкованих font-metrics, який зсуває inline-block кнопку по baseline незалежно від стилів самої кнопки. Рішення: `className="contents"` (display:contents) на `<form>`, щоб вона зникла з розкладки і кнопка стала прямим флекс-елементом рядка — і НЕ загортати `DeleteButton` в додатковий `<div>` (та сама проблема відтворюється на рівень вище). Також знадобився `appearance-none` на самій кнопці — нативний рендеринг `<button>` в Chrome/Edge резервує внутрішній відступ, який не прибирається через `padding: 0`.
- **Читання localStorage при монтуванні компонента**: `useState` + `useEffect(() => setState(...), [])` ловить лінт-правило `react-hooks/set-state-in-effect` І ризикує гідратаційним розсинхроном (сервер рендерить порожнє, клієнт після ефекту — реальні дані, є проміжний "блимання"). Правильне рішення — `useSyncExternalStore` з окремим `getServerSnapshot` (повертає порожній стан на сервері) — React сам синхронізує без зайвого рендер-циклу й без гідраційних попереджень. Використано в `src/lib/cart-context.tsx`. **Важливо:** `getServerSnapshot`/`getSnapshot` мають повертати СТАБІЛЬНЕ посилання, коли дані не змінились — `return []` (новий літерал щоразу) ловить попередження React "should be cached to avoid an infinite loop", бо `[] !== []`. Треба модульна константа (`const EMPTY_ITEMS = []`) і повертати саме її.
- **Той самий "setState синхронно в ефекті" трапляється і в debounced-пошуку** (напр. `NovaPoshtaFields`): гілка "умова не виконана — очистити результати" (`setResults([])`) ловить те саме правило. Рішення однакове завжди: НЕ викликати setState у ранньому виході з ефекту — замість цього обчислювати "чи показувати результати" прямо в рендері (похідна змінна на основі тих самих умов), а стейт із застарілими результатами просто ніколи не рендериться.
- **Мокапи будуються дизайн-інструментом (не пряма верстка)**: класи типу `data-r="..."` — це внутрішні хуки прототипу, а не CSS-класи; логіка видимості/стану (типу `{{ topOpacity }}`, `{{ cartCount }}`) — placeholder-биндинги без реальної реалізації, копіювати нема чого — поведінку треба вивести з контексту й запитати, якщо неоднозначно. АЛЕ візуальні деталі (кольори, розміри, паддинги, брейкпоінти, точні значення в `@media`) — це вже конкретний, точний код, який ТРЕБА читати й відтворювати один-в-один, не досмислюючи "на око". Двічі помилився саме так: (1) вигадав "розтягнути кнопку на всю ширину", хоча в `@media (max-width: 640px)` там чітко `width: auto; font-size: 12.5px`; (2) поставив свою обводку/тінь іконці "вгору", хоча в коду `border: none; filter: drop-shadow(...)`. Правило від Стаса 2026-09-24: перш ніж стилізувати щось "по своєму відчуттю" — знайти й прочитати точний код у файлі дизайна (`grep`/`node -e` по HTML), а не здогадуватись із скріншота.
- Публічний сайт (roboty/novyny/shop/cart/checkout) поки НЕ має Header/Footer/CartIcon з `architecture.md` розд. 5.2 — кожна сторінка це окремий `<main>` без спільного шаблону. Це свідомо тимчасово, до окремої майбутньої фази верстки самого сайту.

## Далі

Магазин, погоджений порядок:
1. ~~Категорії (адмін CRUD)~~ — готово
2. ~~Товари (адмін CRUD)~~ — готово
3. ~~Публічні сторінки: каталог → картка товару → кошик → чекаут~~ — готово
4. ~~Замовлення (адмін)~~ — готово
5. ~~Нова пошта (живий пошук)~~ — готово
6. ~~WayForPay (код редиректу + вебхука)~~ — готово, на тестовому мерчанті

**Магазин повністю зібраний і перевірений** (окрім живого редиректу на сторінці WayForPay й повернення з неї — та частина вимагає публічної адреси, деплой свідомо відкладено). Заміна тестового мерчанта WayForPay на бойовий, коли буде підписано договір — лише значення в `.env`, код не чіпати.

Навігація «Магазин» в сайдбарі повністю зібрана (Категорії, Товари, Замовлення).

**Наступна фаза — сам публічний сайт** (за словами Стаса, "перейдем к самому сайту"), розпочато 2026-09-24:
1. ~~Header/Footer/CartIcon~~ (`src/components/layout/header.tsx`, `footer.tsx`) — **готово й підтверджено Стасом 2026-09-24**. Точна відповідність макету: sticky header з backdrop-blur, 6 якорів навігації, робочий перемикач мов (uk/en/ru, зберігає поточний маршрут), кошик з лічильником, Telegram, CTA "Записатися на консультацію", бургер-меню з точними брейкпоінтами з `@media` макету (640px/1024px — flex+space-between з `display:contents` на мобільному, компактна кнопка 12.5px/11×13px). Footer: брендблок, навігація, юридичні посилання на `/legal/*` (сторінки ще не створені), соцмережі, копірайт, плаваюча кнопка "вгору" з маскотом (`assets/butt_up.png` → `public/scroll-top-icon.png`, `border:none`, `drop-shadow`, hover-поворот 360°, з'являється після ~150px скролу / ховається в самому низу — поріг тимчасовий, поки на Головній немає реальних блоків).
2. **Верстка секцій головної сторінки за макетом** (`Viktoriia Lemeshko - Homepage+mobile.html`), розпочато й ведеться строго по одній секції за раз з підтвердженням Стаса перед переходом до наступної. **Готово й підтверджено 2026-09-24**: Hero, Про психолога, Мої послуги (9 карток-акордеонів, реальний контент і SVG-іконки знайдені у вбудованому `serviceData` в самому файлі макета — не хардкодити з нуля, коли бачиш `{{ placeholder }}`, спершу шукати `const xxxData = [...]` у скрипті), Цифри та факти, ТМ Єнот ЕМО, Відгуки, Мої роботи, Як проходить робота (4 кроки, статичний контент — знайдено як `steps:` у тому ж скрипті), Новини та анонси (DB-driven, `NewsPost` через `publishedNewsWhere()`, картка з бейджем категорії ліворуч і датою праворуч — за проханням Стаса зміщено від макета, де вони йшли поспіль зліва; опис — `line-clamp-2`), Відео-привітання (превʼю-заглушка з play-кнопкою — реального відео в макеті нема, це усвідомлено; соцмережі Facebook/Instagram — посилання-заглушки `#`, реальних адрес ніде не вказано), Магазин (DB-driven, `Product` з `showOnHome:true`, той самий градієнтний бейдж категорії `CATEGORY_BADGE_GRADIENT` і `formatPrice`, що й на `/shop`; кнопка «Додати в кошик» реально працює — той самий `useCart`, лише перестильована під цю секцію окремим `HomeAddToCartButton`, щоб не займатись рефакторингом спільного `AddToCartButton`), FAQ (DB-driven, `FaqEntry.showOnHome`, засіяно 5 реальних питань з макета; акордеон — **одиночний** (single-open, відкриття нового автоматично закриває попередній — не як у «Мої послуги», де кожна картка незалежна); кнопка «Задати своє питання» відкриває модалку з формою (ім'я/email/питання) → зберігає в `FaqQuestion` через новий server action `submitFaqQuestion` (`src/server/actions/faq-question.ts`) → показує екран подяки. Відправка email — поки TODO в коді, email-інфраструктури (SMTP/Resend) в проєкті ще нема, узгоджено зі Стасом відкласти. Спрощення від макета: у оригіналі форма-успіх — окремий другий fixed-оверлей (`{{askSent}}`), у мене контент просто перемикається всередині того самого модального вікна — візуальний результат ідентичний, технічно простіше). Плавний скрол на весь сайт (`scroll-behavior: smooth; scroll-padding-top: 96px` — точно з макета). CTA — запис (остання секція) — теж готово: градієнтний блок з формою запису (зберігає в `ConsultationRequest` через `submitConsultationRequest`), той самий паттерн модалки-подяки, що і в FAQ.

   **Головна сторінка повністю готова й підтверджена Стасом 2026-09-24 — десктоп і адаптив.** Після здачі всіх 13 секцій виправлено ще:
   - Реальна висота логотипу в хедері (62px, не 56px), поріг появи кнопки "вгору" (600px/80px, не 150px/4px — точні значення зі скрипта макета), мобільний розмір/позиція кнопки "вгору" (52px/16px), hover-поворот "вгору" (та сама помилка `transform` замість `rotate`, що й раніше в сесії).
   - Якорі "Магазин"/"Новини" в хедері — прибрано зайвий прямий перехід на `/shop`/`/novyny` (застаріле рішення з ранньої архітектури до того, як ці секції зʼявились на Головній) — тепер звичайні якорі `#shop`/`#news`, як у макеті.
   - Футер: комплексний перегляд і виправлення — адаптивні відступи контейнера (не було), колір роздільної лінії (navy/14, не /10), адаптивний gap сітки (22/36/48px, не завжди 48), max-width/full-span бренд-блоку й соцмереж на мобільному, gap навігаційних колонок (13px, не 12), зайвий `transition-colors` на посиланнях (якого нема в макеті — та сама помилка, що раніше в хедері), радіус іконок соцмереж (12px, не 11), відсутня кольорова тінь при наведенні на іконки соцмереж (у кожної своя), точна відстань зсуву при наведенні (-3px). Додано декоративний круг переходу CTA→Footer (`top:-190px`, `rgba(201,48,124,.04)`) — і виявлено та виправлено, що прозора верхівка градієнта футера показувала бежевий `body` замість білого `#FFFDFC` як у макеті (додано другий шар фону `, #FFFDFC` після градієнта).
   - **Реальний баг на мобільному, знайдений через довгий цикл хибних гіпотез**: декоративний круг у Hero (велика розмита пляма) був майже вдвічі вищим за належне на мобільному — причина: пропущене загальне правило макета `div[style*="height: 520px"] { height: 300px !important; }` на ≤640px (застосував це правило до Video-секції, забув застосувати до самого Hero-круга). Перш ніж підозрювати кеш/анімацію/скріншот-інструмент — спочатку звіряти EXACT computed-стилі елемента через DevTools Protocol проти всіх медіа-запитів макета.
   - Фото в Hero на мобільному (<640px) тепер йде одразу після фіолетового підзаголовка, а не після всіх абзаців — через `display:contents` на текстовому блоці (лише на мобільному, `sm:block` повертає звичайний блок на десктопі) + явний CSS `order-1..8` на кожному елементі (точно як `[data-r="herotext"]{display:contents}` + `data-o="N"` в макеті). `order` не впливає на десктопну верстку, бо там контейнер знову стає `block`, а не grid/flex.
   - Перемикання мови (uk/en/ru) більше не скролить сторінку нагору — `router.replace(pathname, {locale, scroll:false})`.
   - **Переклади для всієї Головної (en/ru)** — додано ~13 нових namespace'ів (`HomeHero`, `HomeAbout`, `HomeServices` та ін.) в `messages/{uk,en,ru}.json`. Усі англійські/російські тексти взяті дослівно з вбудованого масиву `i18n = [[uk, en, ru], ...]` у самому файлі макета (не вигадані) — цей масив покриває майже весь статичний контент сайту, варто памʼятати про нього і для майбутніх сторінок. DB-контент (відгуки/роботи/новини/товари/FAQ) і так вже локалізований через `pickLocalized()` з фолбеком на українську, якщо перекладу нема в адмінці — окремо перевірено, що цей фолбек вже коректно працює скрізь.
   - **Відправка email за формами (питання в FAQ, запис на консультацію) — свідомо відкладена**: обидва server actions (`submitFaqQuestion`, `submitConsultationRequest`) мають `TODO` в коді, дані вже коректно зберігаються в БД (`FaqQuestion`, `ConsultationRequest`), не вистачає лише SMTP/Resend-інтеграції, якої в проєкті ще нема.
   - **Важливе відкриття 2026-09-24**: секції «Відгуки», «Мої роботи» та «Новини та анонси» на Головній — НЕ статичний контент, а мають бути DB-driven через вже готові моделі `Testimonial` (`showOnHome`), `PortfolioWork` і `NewsPost` (ті самі, що і в адмінці та на сторінках `/roboty`, `/novyny`) — щоб Вікторія могла редагувати їх без розробника. Спочатку зробив «Відгуки» на хардкодженому масиві з тексту макета — довелось переробити на `prisma.testimonial.findMany({where:{showOnHome:true}})` + засіяти реальні 5 відгуків з макета в БД замість тестових записів. «Мої роботи»/«Новини» одразу зроблено DB-driven; зараз показують тестові записи-заглушки з БД — самі оновляться, коли Стас додасть реальний контент через адмінку. Загальний паттерн: перш ніж верстати секцію зі списком карток на Головній — спочатку перевірити `prisma/schema.prisma`, чи вже існує відповідна модель з готовою адмінкою, а не хардкодити текст із макета.
   - **Обрізання довжини картки під фіксовану висоту** (щоб розділові лінії/футер картки були на одному рівні в ряду незалежно від довжини тексту): для тексту з видимим "..." — `line-clamp-N` (Мої роботи, Новини — `line-clamp-2`, опис із БД); для тексту БЕЗ "..." (Стас: контент модерує адмін, тому "..." не потрібне) — фіксована `h-[lineHeight × N]px` + `overflow-hidden` без `line-clamp` (Відгуки — `h-[138.6px]` = 16.5px×1.68×5 рядків).
   - **Обрізання тексту в картках з довільним (адмін-редагованим) вмістом**: якщо потрібен видимий "..." в кінці — `line-clamp-N` (як на `/roboty`, картка `PortfolioWork.excerpt`). Якщо "..." НЕ потрібен (Стас: "відгуки додаються з адмінки — це задача адміна робити їх з обмеженою кількістю символів") — фіксована висота `h-[lineHeight × N]px` + `overflow-hidden` без `line-clamp` (використано в картці відгуку: `h-[138.6px]` = 16.5px×1.68×5 рядків). Другий спосіб додатково вирівнює розділові лінії під текстом на одному рівні між картками одного ряду незалежно від довжини тексту (бо резервує однакову висоту завжди, а не тільки для довгого тексту).
3. Ревізія вже готових сторінок (roboty/novyny/shop/cart/checkout) під точнішу відповідність макетам (раніше свідомо не гнались за пиксель-перфектом на цих сторінках, за домовленістю зі Стасом, тепер вимога — повна відповідність).
4. **Email-сповіщення за формами** (питання в FAQ → `FaqQuestion`, запис на консультацію → `ConsultationRequest`) — дані вже зберігаються в БД, не вистачає лише SMTP/Resend-інтеграції. Свідомо відкладено, підтверджено Стасом 2026-09-24.

## Ідеї на майбутнє (не зараз, свідомо відкладено)

- **Автоматичне створення ТТН через API Нової пошти**: зараз (і за узгодженим планом навіть після підключення ключа НП) Вікторія вручну створює накладну у своєму кабінеті НП і вручну тисне «Позначити відправленим» в адмінці (architecture.md, розд. 5.4). Повна інтеграція (автостворення ТТН, автоматичний перехід у SHIPPED) — окрема, значно більша задача; свідомо не робимо зараз.
- **Накладений платіж (оплата при отриманні)**: зараз магазин рахує лише повну передоплату, і статусна модель Order (`PENDING_PAYMENT → PAID → SHIPPED → CANCELLED`) на це й розрахована (відправка можлива тільки зі статусу PAID). Якщо пізніше з'явиться накладений платіж, знадобиться або окреме поле способу оплати, або додатковий статус (типу "Відправлено, очікує оплату при отриманні") — поточний enum для такого сценарію не підходить. Обговорювали зі Стасом 2026-09-24, вирішили не закладати наперед.
