# Прогрес розробки — Ресурс 1 (сайт-візитка Вікторії Лемешко)

Робочий журнал: що вже зроблено, які рішення були прийняті і чому, що далі. Оновлюється в кінці кожної сесії. Деталі архітектурних рішень — в `architecture.md`, вимоги — в `tech_specs.md`.

## Зроблено

### Інфраструктура
- Next.js (App Router) + TypeScript + Prisma 6.19.3 (навмисно не Prisma 7 — там прибрали `datasource.url`, ламає поточний підхід) + PostgreSQL.
- Auth.js v5, сесії через **JWT** (не database — Credentials-провайдер це вимагає).
- next-intl (uk за замовчуванням, en/ru); в адмінці локалізація вимкнена — тільки українська.
- Файлове сховище — self-hosted MinIO (S3-сумісне) на сервері Стаса. Це фінальне рішення й для продакшена, не тільки для розробки. В БД зберігаються відносні ключі, а не повні URL (`getPublicUrl()`), щоб можна було міняти розташування сховища без міграції даних.
- Tailwind v4, дизайн-токени в `globals.css` (`@theme`), кольори з дизайн-системи (navy/coral/magenta/violet/blue/indigo/powder).

### Адмінка + публічні сторінки (vertical slices)
Готові повністю (CRUD в адмінці + відповідні публічні сторінки):
- **Мої роботи** (портфоліо)
- **Новини та анонси** — з відкладеною публікацією (без нового поля в БД: існуюче поле `date` використовується як фільтр видимості, `publishedNewsWhere()`)
- **Відгуки**
- **Дипломи** — з перемикачем «Показувати на сайті» (`showOnSite`)
- **FAQ** — з перемикачем «Показувати на головній сторінці» (`showOnHome`)

Готові в адмінці:
- **Категорії** — назва (3 мови) + слаг; картки в списку, 5 в ряд, з мініатюрою першого товару категорії + датою створення; видалення заблоковане, поки в категорії є товари
- **Товари** — назва/тип/склад/опис (3 мови кожне), категорія, ціна, головне фото (обов'язкове), галерея (необов'язково), перемикач «Показувати на головній»; картки 4 в ряд (8 на сторінку — 2 ряди, влазить на екран без скролу), фільтр за категорією + «Знайдено: N»; форма блокує створення товару, поки немає жодної категорії

Готові публічні сторінки магазину (`/shop`, `/shop/[slug]`, `/cart`, `/checkout`, `/checkout/pay`, `/checkout/success`):
- **Каталог** — фільтр за категоріями (динамічний, з БД), сітка 4 в ряд, картка товару з градієнтним бейджем категорії поверх фото (точна специфікація з макета), пагінація
- **Картка товару** — галерея з клікабельними мініатюрами, опис, Тип товару/Характеристики, ціна + лічильник «У кошику» + степер кількості, «Рекомендовані товари» (та ж категорія)
- **Кошик** — **поки повністю на localStorage** (не БД): свого логіну покупця до чекауту не існує (акаунт створюється лише під час оформлення замовлення), тому "БД-кошик, прив'язаний до сесії" з `architecture.md` розд. 5.2 поки не застосовний — узгоджено з користувачем. `CartProvider` на `useSyncExternalStore` (не `useState`+`useEffect` — уникає гідратаційного розсинхрону І `react-hooks/set-state-in-effect`)
- **Чекаут** — форма контактів + доставки + резюме замовлення повністю робоча, `createOrder` створює `User` (за email, +роль `PURCHASER`) і `Order`/`OrderItem` в БД зі статусом `PENDING_PAYMENT`.
  - **Місто/відділення Нової пошти — живий пошук через API** (`NovaPoshtaFields`, проксі `/api/nova-poshta/{cities,warehouses}`, ключ `NOVA_POSHTA_API_KEY` в `.env`, тільки на сервері) — підключено 2026-09-24; довідникове API (getCities/getWarehouses) не вимагає підписаного договору, на відміну від реального створення накладних.
  - **WayForPay підключений на тестовому мерчанті** (`test_merch_n1` — публічний демо-акаунт із офіційної документації WayForPay, не приватний секрет) — підключено й **перевірено 2026-09-24**: створив тестове замовлення в БД, `/checkout/pay` коректно згенерував форму з вірним підписом HMAC_MD5 (`src/lib/wayforpay.ts`); змоделював реальний вебхук-запит із правильною підписом на `/api/payments/wayforpay/callback` — сервер прийняв, відповів коректним ack, і замовлення реально перейшло в `PAID` (з `paidAt`/`wayforpayStatus`) у БД; `/checkout/success` показав правильний статус. Не перевірявся лише сам живий редирект на сторінку WayForPay й повернення звідти (це вже їхня сторона — вимагає публічної адреси, деплой свідомо відкладено). Коли буде реальний договір — заміна лише значень у `.env` (`WAYFORPAY_MERCHANT_ACCOUNT`/`_SECRET_KEY`/`_DOMAIN`), код чіпати не треба.

Публічний сайт поки без header/footer/навігації — це окрема майбутня фаза "сам сайт" (за словами Стаса), тому на ці сторінки можна потрапити лише напряму по URL.

Готова адмінка **Замовлення** (`/admin/zamovlennia`):
- Список: комбіновані фільтри (не таби — вони мають діяти одночасно, а не перемикати вид) — Період від/до, Статус, Категорія, Товар, через звичайну GET-форму (без JS); «Знайдено: N»; номер + бейдж статусу + клієнт + дата + сума; пагінація зберігає активні фільтри
- Картка замовлення: товари з сумою по кожному, контакти, реальні дані доставки з Нової пошти
- Зміна статусу — **ручні кнопки** (Позначити оплаченим → Позначити відправленим, Скасувати), а не вільний select. «Оплачено» тепер МОЖЕ проставлятись і автоматично через WayForPay-вебхук (коли той запрацює наживо) — ручна кнопка лишається як fallback/для готівки поза системою, кнопки для «Відправлено» так і залишаться ручними завжди (Вікторія відправляє фізично сама)

Усі форми адмінки побудовані на єдиному, вже відпрацьованому паттерні:
- `useActionState` + серверні екшени, що повертають `{ fieldErrors, values }`
- Живе зникнення помилки поля при заповненні
- `LocaleTabs` для трьох мовних версій тексту
- `PhotoPicker` для обов'язкового фото (уся головна фотографія — завжди обов'язкова, це принципове рішення)
- Кастомна серверна валідація замість браузерних popup (`noValidate`)

### UX-полірування адмінки
- Уніфіковані кольори кнопок: `primaryButtonClass` (індиго) / `secondaryButtonClass` (magenta hover — це підтверджено як єдиний правильний hover-колір по всіх реальних макетах, не indigo)
- Курсор-«рука» на всіх кнопках і посиланнях глобально
- Приховані скролбари всюди, окрім `textarea`
- Бейдж **«Приховано»** (сірий, `text-navy-soft bg-navy/5`) додано в списки Відгуків і FAQ — тепер однаково з Дипломами, візуально відрізняється від «На головній» (indigo)
- Підсвітка активного пункту меню в сайдбарі (лише magenta текст, без фону) — винесено в окремий client-компонент `AdminSidebarNav`, бо потрібен `usePathname()`. «Магазин» (розкривний пункт з підпунктами Категорії/Товари) підсвічується строго по маршруту, так само як і решта пунктів — розгортання/згортання суто механічне (стрілка), без власного кольорового стану, інакше він конфліктував з підсвіткою реальної поточної сторінки
- Категорійний градієнтний бейдж (`linear-gradient(120deg, #F2662F 0%, #C9307C 42%, #7A3AA0 70%, #2B6BB8 100%)`, білий uppercase-текст) — точна специфікація з `design/VL - Shop (client).html`, використовується на картках категорій поверх мініатюри товару
- **Головна сторінка адмінки («Актуальні показники»)** — раніше була пустою заглушкою, тепер дашборд з віджетами (`src/components/admin/stat-card.tsx` — `StatCard`/`PeriodFilterBar`), три секції: «Що потребує уваги» (замовлення, оплачені й ще не відправлені — клікабельно у «Замовлення» з фільтром `?status=PAID`; нові заявки FAQ+консультації, з фільтром періоду сьогодні/7/30 днів/весь час), «Мої фінансові показники» (виручка + розбивка замовлень за статусами, свій фільтр період місяць/весь час), «Мій контент на сайті» (лічильники товарів/робіт/новин/відгуків, компактні картки — назва й число в один рядок, нижчі за решту). Кліковані картки — hover: бордер і назва в magenta/40 (не indigo — за проханням Стаса indigo "не дуже смотрівся" тут). Ключові рішення узгоджені зі Стасом: назви секцій — не скорочені до одного слова, а навпаки розгорнуті/"живі" ("Мої фінансові показники", не просто "Фінанси" — після кількох невдалих спроб зрозуміти, що саме мається на увазі під "лаконічно").
- **Вхід в адмінку — логін замість email**: поле `User.email` лишилось як є в схемі (спільне для Ресурсів 1–3, перейменування зачепило б увесь крос-застосунковий SSO — це вже за межі "поміняти форму входу"), але форма (`/admin/login`) і `authorize()` у `src/lib/auth.ts` більше не вимагають формату email (`type="email"` → `type="text"`) — там просто унікальний рядок, БД ніяк не перевіряє, що це реальна пошта. Реальний обліковий запис Вікторії (id `cmuctplpl0000w44865r7zy9k`) оновлено напряму в БД: логін `Vikki`, новий пароль (заданий Стасом, у git не зберігається). Тестовий `test-admin@example.com`/`ADMIN_EMAIL`/`ADMIN_PASSWORD` у `.env` синхронізовано з новими значеннями (на випадок повторного запуску `db:seed` — там `upsert` за `email`, і без синхронізації створило б ДРУГИЙ обліковий запис поруч зі старим).
- **Футер: кредит розробника** — по центру нижнього рядка (десктоп: `grid-cols-3`, копірайт зліва / кредит по центру / торгова марка справа — `justify-between` тут не годиться, бо не гарантує реальний центр при різній довжині лівого/правого тексту; мобільний: стовпчик) напис «Розробка та підтримка:» (переклад на всі 3 мови, `Footer.developedBy`) + лого ASDev (`assets/logo_dev.png` → `public/logo-dev.png`), увесь блок — посилання на `https://asdev.online/`, лого трохи збільшується при наведенні (`group-hover:scale-110`).

## Важливі особливості / підводні камені

- **Windows EPERM при `prisma generate`**: якщо в цей момент запущений `npm run dev`, він тримає відкритим `query_engine-windows.dll.node`, і Prisma Client не перегенеровується повністю. Міграції БД завжди проходять коректно через SQL, але сам JS-клієнт треба регенерувати вручну — Стасу потрібно повністю зупинити dev-сервер і перезапустити. Один раз це реально спричинило видиму помилку (поле `showOnSite` показувало не те значення, поки не перезапустили).
- **Дисципліна по обсягу змін**: правити рівно те, що попросили; питати перед будь-яким дизайн/UX-рішенням або розширенням обсягу. Функціональні наслідки вже узгодженого рішення — уточнювати не треба, можна одразу робити.
- Файлові інпути не можна відновити через `defaultValue` — тільки зберігати сам `File` в state і повертати через `DataTransfer`.
- Радіобатони й інші контрольовані інпути після Server Action React може «не домалювати» в DOM — рішення: `defaultChecked`/`defaultValue` (uncontrolled) замість `checked`/`value`.
- **`<button>` всередині `<form>` у флекс-рядку (напр. «Редагувати посилання + Видалити кнопка») може візуально «не стояти на одній лінії» з сусіднім `<a>`**, навіть якщо в button скинуті всі padding/border/font і computed-стилі співпадають один-в-один. Причина — `<form>` (або будь-яка проміжна block-обгортка) є справжнім флекс-елементом, а не сама кнопка всередині неї; блочна обгортка має власний line-box "струт", прив'язаний до успадкованих font-metrics, який зсуває inline-block кнопку по baseline незалежно від стилів самої кнопки. Рішення: `className="contents"` (display:contents) на `<form>`, щоб вона зникла з розкладки і кнопка стала прямим флекс-елементом рядка — і НЕ загортати `DeleteButton` в додатковий `<div>` (та сама проблема відтворюється на рівень вище). Також знадобився `appearance-none` на самій кнопці — нативний рендеринг `<button>` в Chrome/Edge резервує внутрішній відступ, який не прибирається через `padding: 0`.
- **Читання localStorage при монтуванні компонента**: `useState` + `useEffect(() => setState(...), [])` ловить лінт-правило `react-hooks/set-state-in-effect` І ризикує гідратаційним розсинхроном (сервер рендерить порожнє, клієнт після ефекту — реальні дані, є проміжний "блимання"). Правильне рішення — `useSyncExternalStore` з окремим `getServerSnapshot` (повертає порожній стан на сервері) — React сам синхронізує без зайвого рендер-циклу й без гідраційних попереджень. Використано в `src/lib/cart-context.tsx`. **Важливо:** `getServerSnapshot`/`getSnapshot` мають повертати СТАБІЛЬНЕ посилання, коли дані не змінились — `return []` (новий літерал щоразу) ловить попередження React "should be cached to avoid an infinite loop", бо `[] !== []`. Треба модульна константа (`const EMPTY_ITEMS = []`) і повертати саме її.
- **Той самий "setState синхронно в ефекті" трапляється і в debounced-пошуку** (напр. `NovaPoshtaFields`): гілка "умова не виконана — очистити результати" (`setResults([])`) ловить те саме правило. Рішення однакове завжди: НЕ викликати setState у ранньому виході з ефекту — замість цього обчислювати "чи показувати результати" прямо в рендері (похідна змінна на основі тих самих умов), а стейт із застарілими результатами просто ніколи не рендериться.
- **Мокапи будуються дизайн-інструментом (не пряма верстка)**: класи типу `data-r="..."` — це внутрішні хуки прототипу, а не CSS-класи; логіка видимості/стану (типу `{{ topOpacity }}`, `{{ cartCount }}`) — placeholder-биндинги без реальної реалізації, копіювати нема чого — поведінку треба вивести з контексту й запитати, якщо неоднозначно. АЛЕ візуальні деталі (кольори, розміри, паддинги, брейкпоінти, точні значення в `@media`) — це вже конкретний, точний код, який ТРЕБА читати й відтворювати один-в-один, не досмислюючи "на око". Двічі помилився саме так: (1) вигадав "розтягнути кнопку на всю ширину", хоча в `@media (max-width: 640px)` там чітко `width: auto; font-size: 12.5px`; (2) поставив свою обводку/тінь іконці "вгору", хоча в коду `border: none; filter: drop-shadow(...)`. Правило від Стаса 2026-09-24: перш ніж стилізувати щось "по своєму відчуттю" — знайти й прочитати точний код у файлі дизайна (`grep`/`node -e` по HTML), а не здогадуватись із скріншота.
- Публічний сайт (roboty/novyny/shop/cart/checkout) поки НЕ має Header/Footer/CartIcon з `architecture.md` розд. 5.2 — кожна сторінка це окремий `<main>` без спільного шаблону. Це свідомо тимчасово, до окремої майбутньої фази верстки самого сайту.
- **Скрол у вкладеному `overflow-y-auto` контейнері (не window) під час SPA-переходів у Next.js поводиться непередбачувано**: адмінка спочатку була зверстана як сайдбар + `<main style="overflow-y-auto">` (замість скролу вікна, як на публічній частині) — довгі сторінки після переходу з сайдбару відкривались не з самого верху. За документацією `<Link>` (`node_modules/next/dist/docs/.../link.md`) Next.js за замовчуванням **зберігає** позицію скролу при переході, якщо цільова сторінка вже "видна у вʼюпорті" (як наш завжди видимий `<main>`) — тобто успадкований scrollTop із попередньої сторінки лишається як є, це не баг, а документована поведінка "як back/forward у браузері". Ручні спроби скинути `scrollTop` вручну (ефект на зміну `pathname`, потім `key={pathname}` для примусового ремаунту `<main>`) не спрацювали стабільно — ще й давали видиме "смикання". Правильне рішення: прибрати саму архітектурну відмінність від публічної частини — сайдбар став `sticky`, скролиться вікно цілком, і переюзано вже перевірений `ScrollResetOnNavigate`-патерн (`src/components/admin/scroll-reset.tsx`, `window.scrollTo` + тимчасове вимкнення `html{scroll-behavior:smooth}` з `globals.css`, спільного з публічною частиною). **Урок**: коли публічна й адмінська частина щось роблять по-різному і в одній є баг, якого нема в іншій — перш ніж винаходити окремий фікс під архітектуру з багом, спитати, чи не простіше усунути саму архітектурну різницю.

## Далі

Магазин, погоджений порядок:
1. ~~Категорії (адмін CRUD)~~ — готово
2. ~~Товари (адмін CRUD)~~ — готово
3. ~~Публічні сторінки: каталог → картка товару → кошик → чекаут~~ — готово
4. ~~Замовлення (адмін)~~ — готово
5. ~~Нова пошта (живий пошук)~~ — готово
6. ~~WayForPay (код редиректу + вебхука)~~ — готово, на тестовому мерчанті

**Магазин повністю зібраний і перевірений** (окрім живого редиректу на сторінці WayForPay й повернення з неї — та частина вимагає публічної адреси, деплой свідомо відкладено). Заміна тестового мерчанта WayForPay на бойовий, коли буде підписано договір — лише значення в `.env`, код не чіпати.

Навігація «Магазин» в сайдбарі повністю зібрана (Категорії, Товари, Замовлення).

**Наступна фаза — сам публічний сайт** (за словами Стаса, "перейдем к самому сайту"), розпочато 2026-09-24:
1. ~~Header/Footer/CartIcon~~ (`src/components/layout/header.tsx`, `footer.tsx`) — **готово й підтверджено Стасом 2026-09-24**. Точна відповідність макету: sticky header з backdrop-blur, 6 якорів навігації, робочий перемикач мов (uk/en/ru, зберігає поточний маршрут), кошик з лічильником, Telegram, CTA "Записатися на консультацію", бургер-меню з точними брейкпоінтами з `@media` макету (640px/1024px — flex+space-between з `display:contents` на мобільному, компактна кнопка 12.5px/11×13px). Footer: брендблок, навігація, юридичні посилання на `/legal/*` (сторінки ще не створені), соцмережі, копірайт, плаваюча кнопка "вгору" з маскотом (`assets/butt_up.png` → `public/scroll-top-icon.png`, `border:none`, `drop-shadow`, hover-поворот 360°, з'являється після ~150px скролу / ховається в самому низу — поріг тимчасовий, поки на Головній немає реальних блоків).
2. **Верстка секцій головної сторінки за макетом** (`Viktoriia Lemeshko - Homepage+mobile.html`), розпочато й ведеться строго по одній секції за раз з підтвердженням Стаса перед переходом до наступної. **Готово й підтверджено 2026-09-24**: Hero, Про психолога, Мої послуги (9 карток-акордеонів, реальний контент і SVG-іконки знайдені у вбудованому `serviceData` в самому файлі макета — не хардкодити з нуля, коли бачиш `{{ placeholder }}`, спершу шукати `const xxxData = [...]` у скрипті), Цифри та факти, ТМ Єнот ЕМО, Відгуки, Мої роботи, Як проходить робота (4 кроки, статичний контент — знайдено як `steps:` у тому ж скрипті), Новини та анонси (DB-driven, `NewsPost` через `publishedNewsWhere()`, картка з бейджем категорії ліворуч і датою праворуч — за проханням Стаса зміщено від макета, де вони йшли поспіль зліва; опис — `line-clamp-2`), Відео-привітання (превʼю-заглушка з play-кнопкою — реального відео в макеті нема, це усвідомлено; соцмережі Facebook/Instagram — посилання-заглушки `#`, реальних адрес ніде не вказано), Магазин (DB-driven, `Product` з `showOnHome:true`, той самий градієнтний бейдж категорії `CATEGORY_BADGE_GRADIENT` і `formatPrice`, що й на `/shop`; кнопка «Додати в кошик» реально працює — той самий `useCart`, лише перестильована під цю секцію окремим `HomeAddToCartButton`, щоб не займатись рефакторингом спільного `AddToCartButton`), FAQ (DB-driven, `FaqEntry.showOnHome`, засіяно 5 реальних питань з макета; акордеон — **одиночний** (single-open, відкриття нового автоматично закриває попередній — не як у «Мої послуги», де кожна картка незалежна); кнопка «Задати своє питання» відкриває модалку з формою (ім'я/email/питання) → зберігає в `FaqQuestion` через новий server action `submitFaqQuestion` (`src/server/actions/faq-question.ts`) → показує екран подяки. Відправка email — поки TODO в коді, email-інфраструктури (SMTP/Resend) в проєкті ще нема, узгоджено зі Стасом відкласти. Спрощення від макета: у оригіналі форма-успіх — окремий другий fixed-оверлей (`{{askSent}}`), у мене контент просто перемикається всередині того самого модального вікна — візуальний результат ідентичний, технічно простіше). Плавний скрол на весь сайт (`scroll-behavior: smooth; scroll-padding-top: 96px` — точно з макета). CTA — запис (остання секція) — теж готово: градієнтний блок з формою запису (зберігає в `ConsultationRequest` через `submitConsultationRequest`), той самий паттерн модалки-подяки, що і в FAQ.

   **Головна сторінка повністю готова й підтверджена Стасом 2026-09-24 — десктоп і адаптив.** Після здачі всіх 13 секцій виправлено ще:
   - Реальна висота логотипу в хедері (62px, не 56px), поріг появи кнопки "вгору" (600px/80px, не 150px/4px — точні значення зі скрипта макета), мобільний розмір/позиція кнопки "вгору" (52px/16px), hover-поворот "вгору" (та сама помилка `transform` замість `rotate`, що й раніше в сесії).
   - Якорі "Магазин"/"Новини" в хедері — прибрано зайвий прямий перехід на `/shop`/`/novyny` (застаріле рішення з ранньої архітектури до того, як ці секції зʼявились на Головній) — тепер звичайні якорі `#shop`/`#news`, як у макеті.
   - Футер: комплексний перегляд і виправлення — адаптивні відступи контейнера (не було), колір роздільної лінії (navy/14, не /10), адаптивний gap сітки (22/36/48px, не завжди 48), max-width/full-span бренд-блоку й соцмереж на мобільному, gap навігаційних колонок (13px, не 12), зайвий `transition-colors` на посиланнях (якого нема в макеті — та сама помилка, що раніше в хедері), радіус іконок соцмереж (12px, не 11), відсутня кольорова тінь при наведенні на іконки соцмереж (у кожної своя), точна відстань зсуву при наведенні (-3px). Додано декоративний круг переходу CTA→Footer (`top:-190px`, `rgba(201,48,124,.04)`) — і виявлено та виправлено, що прозора верхівка градієнта футера показувала бежевий `body` замість білого `#FFFDFC` як у макеті (додано другий шар фону `, #FFFDFC` після градієнта).
   - **Реальний баг на мобільному, знайдений через довгий цикл хибних гіпотез**: декоративний круг у Hero (велика розмита пляма) був майже вдвічі вищим за належне на мобільному — причина: пропущене загальне правило макета `div[style*="height: 520px"] { height: 300px !important; }` на ≤640px (застосував це правило до Video-секції, забув застосувати до самого Hero-круга). Перш ніж підозрювати кеш/анімацію/скріншот-інструмент — спочатку звіряти EXACT computed-стилі елемента через DevTools Protocol проти всіх медіа-запитів макета.
   - Фото в Hero на мобільному (<640px) тепер йде одразу після фіолетового підзаголовка, а не після всіх абзаців — через `display:contents` на текстовому блоці (лише на мобільному, `sm:block` повертає звичайний блок на десктопі) + явний CSS `order-1..8` на кожному елементі (точно як `[data-r="herotext"]{display:contents}` + `data-o="N"` в макеті). `order` не впливає на десктопну верстку, бо там контейнер знову стає `block`, а не grid/flex.
   - Перемикання мови (uk/en/ru) більше не скролить сторінку нагору — `router.replace(pathname, {locale, scroll:false})`.
   - **Переклади для всієї Головної (en/ru)** — додано ~13 нових namespace'ів (`HomeHero`, `HomeAbout`, `HomeServices` та ін.) в `messages/{uk,en,ru}.json`. Усі англійські/російські тексти взяті дослівно з вбудованого масиву `i18n = [[uk, en, ru], ...]` у самому файлі макета (не вигадані) — цей масив покриває майже весь статичний контент сайту, варто памʼятати про нього і для майбутніх сторінок. DB-контент (відгуки/роботи/новини/товари/FAQ) і так вже локалізований через `pickLocalized()` з фолбеком на українську, якщо перекладу нема в адмінці — окремо перевірено, що цей фолбек вже коректно працює скрізь.
   - **Відправка email за формами (питання в FAQ, запис на консультацію) — свідомо відкладена**: обидва server actions (`submitFaqQuestion`, `submitConsultationRequest`) мають `TODO` в коді, дані вже коректно зберігаються в БД (`FaqQuestion`, `ConsultationRequest`), не вистачає лише SMTP/Resend-інтеграції, якої в проєкті ще нема.
   - **Важливе відкриття 2026-09-24**: секції «Відгуки», «Мої роботи» та «Новини та анонси» на Головній — НЕ статичний контент, а мають бути DB-driven через вже готові моделі `Testimonial` (`showOnHome`), `PortfolioWork` і `NewsPost` (ті самі, що і в адмінці та на сторінках `/roboty`, `/novyny`) — щоб Вікторія могла редагувати їх без розробника. Спочатку зробив «Відгуки» на хардкодженому масиві з тексту макета — довелось переробити на `prisma.testimonial.findMany({where:{showOnHome:true}})` + засіяти реальні 5 відгуків з макета в БД замість тестових записів. «Мої роботи»/«Новини» одразу зроблено DB-driven; зараз показують тестові записи-заглушки з БД — самі оновляться, коли Стас додасть реальний контент через адмінку. Загальний паттерн: перш ніж верстати секцію зі списком карток на Головній — спочатку перевірити `prisma/schema.prisma`, чи вже існує відповідна модель з готовою адмінкою, а не хардкодити текст із макета.
   - **Обрізання довжини картки під фіксовану висоту** (щоб розділові лінії/футер картки були на одному рівні в ряду незалежно від довжини тексту): для тексту з видимим "..." — `line-clamp-N` (Мої роботи, Новини — `line-clamp-2`, опис із БД); для тексту БЕЗ "..." (Стас: контент модерує адмін, тому "..." не потрібне) — фіксована `h-[lineHeight × N]px` + `overflow-hidden` без `line-clamp` (Відгуки — `h-[138.6px]` = 16.5px×1.68×5 рядків).
   - **Обрізання тексту в картках з довільним (адмін-редагованим) вмістом**: якщо потрібен видимий "..." в кінці — `line-clamp-N` (як на `/roboty`, картка `PortfolioWork.excerpt`). Якщо "..." НЕ потрібен (Стас: "відгуки додаються з адмінки — це задача адміна робити їх з обмеженою кількістю символів") — фіксована висота `h-[lineHeight × N]px` + `overflow-hidden` без `line-clamp` (використано в картці відгуку: `h-[138.6px]` = 16.5px×1.68×5 рядків). Другий спосіб додатково вирівнює розділові лінії під текстом на одному рівні між картками одного ряду незалежно від довжини тексту (бо резервує однакову висоту завжди, а не тільки для довгого тексту).
3. **Ревізія вже готових сторінок під точну відповідність макетам** — **повністю готово й підтверджено Стасом 2026-09-25** (roboty, novyny, shop/каталог, картка товару, кошик, чекаут), порядок узгоджено зі Стасом: спочатку «Мої роботи» і «Новини», потім увесь магазин.
   - ~~«Мої роботи» (`/roboty`, `/roboty/[slug]`)~~ — **готово, підтверджено Стасом 2026-09-25**. Список: клієнтська пагінація (4/сторінка на мобільному, 6 на десктопі — точно як у скрипті макета, `w<=640?4:6`), картки з реальними фото замість плейсхолдерів, стрілки+нумеровані пігулки пагінації (не прості лінки, як було). Деталі: галерея з реальних фото (`work.gallery`) зі "ковзним вікном" (3 фото на десктопі, 2 на мобільному — окремий поріг для кожного, `hasArrows`/розмір вікна залежать від брейкпоінта, інакше стрілки не з'являлись і 4-5-те фото були недосяжні на мобільному) + лайтбокс-модалка з навігацією, кнопка «Поділитися» (FB/Telegram-заглушки + копіювання посилання), навігація між роботами, сайдбар з іншими роботами (мобільна горизонтальна scroll-carousel, стрілки листання — той самий `display:contents`+`order` прийом, що і в шапці «Фотогалерея», щоб стрілки стояли в один ряд із заголовком праворуч, а не окремим рядком). `ShareButton` виніс у `src/components/ui/` — спільний для Roboty і Novyny.
   - ~~«Новини» (`/novyny`, `/novyny/[slug]`)~~ — **готово, підтверджено Стасом 2026-09-25**. Той самий патерн, що й «Мої роботи», плюс фільтр за рубриками (Усі/Анонс/Новина/Для психологів) — тепер стилізовані пігулки замість адмінських прямокутних кнопок, фільтрація повністю на клієнті (як у макеті), а не через `?category=` в URL. Сайдбар на сторінці новини має свій власний, окремий фільтр рубрик (менші пігулки, 12.5px) — не плутати зі списком; той самий фікс стрілок листання, що й у «Мої роботи».
   - **Знайдено й виправлено баг у щойно написаній сторінці Work detail**: `lg:px-8` (32px) був помилково написаний як `lg:px-6` (24px) — впіймано під час звірки з News detail (той самий шаблон), не користувачем.
   - **Галерея «Фотогалерея» на Work detail — повна переробка мобільної розкладки**: спочатку зверстав заголовок і стрілки/трек як два окремі, ніколи не об'єднані ряди — на мобільному в макеті вони насправді один флекс-контекст через `[data-r="galwrap"]{display:contents}` на ≤640px, що розгортає стрілки прямо в один ряд із заголовком (`order`), а трек фото переносить на новий рядок на всю ширину. Без цього трек фото не мав власної визначеної ширини (циклічна залежність — кожна мініатюра рахувала 50% від ширини контейнера, який сам залежав від дітей), звідки і "сплюснуті"/зниклі фото. Переписано під той самий прийом `display:contents`+`order`, що вже використовувався для фото в Hero на Головній.
   - **Сайдбари «Інші роботи»/«Інші новини» — та сама проблема зі стрілками**: стрілки листання були в окремому ряду з `justify-between` (розтягнуті по краях), хоча в макеті вони стоять одразу праворуч від заголовка секції в тому самому ряду (знову `display:contents`+`order` на `[data-r="sidearr"]`/`aside`). Заодно виправлено посилання «Усі роботи»/«Усі новини» — на мобільному в макеті воно теж зміщується праворуч (`margin: 18px 0 0 auto`), було зліва.
   - **Загальний сайтовий баг: анімований скрол нагору при переході між сторінками** — макет має `html{scroll-behavior:smooth}` буквально з оригіналу (там кожна сторінка — окремий файл, тому в прототипі це ніколи не проявлялось як анімація). У Next.js SPA-навігації `<html>` лишається тим самим елементом між переходами, і власне скидання скролу нагору при зміні маршруту (Головна→«Мої роботи»/«Новини», список→картка) успадковує smooth і виглядає як "стрічка їде нагору" замість миттєвого відкриття сторінки. Виправлено новим `ScrollResetOnNavigate` (`src/components/layout/scroll-reset.tsx`, підключено в `src/app/[locale]/layout.tsx`) — на кожній зміні `pathname` без хеша інлайново вимикає `scroll-behavior` на ~400мс (з повторним `scrollTo(0,0)`, бо Next.js може ще раз сам скинути скрол уже після монтування нової сторінки, поки контент стрімиться) і повертає назад. Плавний скрол по якорях на самій сторінці (пункти меню, FAQ і т.д.) не зачіпається — ефект спрацьовує лише на зміну URL без хеша.
4. ~~Email-сповіщення за формами~~ (питання в FAQ → `FaqQuestion`, запис на консультацію → `ConsultationRequest`) — **готово й підтверджено Стасом 2026-09-26** (тестовий лист дійшов Вікторії). Свідомо не Resend/SendGrid — обмеження безкоштовних тарифів, а платити за окремий email-сервіс Вікторії немає сенсу, коли на її ж хостингу домену (adm.tools) поштова скринька вже безкоштовна в межах оплаченого хостинг-акаунту (не плутати з їхньою окремою платною послугою «Корпоративна пошта» — це інший, дорожчий продукт, не потрібен для однієї технічної скриньки). Створено `noreply@lemeshko.org` саме як «Хостинг-акаунт», не «Корпоративна пошта». `src/lib/mail.ts` — `nodemailer` + SMTP (`mail.adm.tools:465`, з `.env`: `SMTP_HOST/PORT/USER/PASSWORD`, `MAIL_TO=vik_vik@ukr.net`), викликається з обох server actions (`faq-question.ts`, `consultation-request.ts`) з `replyTo` на email заявника (Вікторія відповідає одним кліком зі своєї пошти), обгорнуто в `try/catch` — збій відправки листа не повинен ламати збереження заявки в БД.
5. ~~Юридичні сторінки~~ — **готово й підтверджено Стасом 2026-09-26**, деталі нижче.

**Ревізія магазину** (крок 3, продовження — після roboty/novyny), сторінка за сторінкою з підтвердженням Стаса:
- ~~Каталог (`/shop`)~~ — **готово, підтверджено Стасом 2026-09-25**. Header-секція з градієнтом і 2 декоративними фігурами (точні координати з `design/VL - Shop (client).html` — інші, ніж на roboty/novyny), ейбров «МАГАЗИН» (колір coral, не violet). Фільтр за категоріями — стилізовані пігулки замість старих адмінських прямокутних кнопок, повністю на клієнті (реальні категорії з БД + «Усі»), назва категорії — `uppercase` (окреме прохання Стаса, застосовано скрізь на публічній частині: бейдж на картці каталогу/рекомендованих/головної, пігулки фільтра, ейбров на сторінці товару — адмінка не чіпалась, там звичайна таблиця даних, не бейдж/список). На ≤1100px весь headrow (текст+фільтр) переходить у колонку — окремий брейкпоінт, не збігається зі стандартними 640/1024. Сітка каталогу — 4/3/2/1 колонок на 1100/1024/640px (4 рівні, а не 2, як було). Пагінація — стрілки+нумеровані пігулки, `perPage = ширина≤640 ? 4 : 12` — точна формула зі скрипта макета (`this.state.w <= 640 ? 4 : 12`), не вигадана. Картка товару (`ProductCard`, спільна для каталогу й рекомендованих) — виправлені паддинги (20/22/22px, не 20 звідусіль), розмір заголовка (17px, не 18), ціни (19px), бейджа категорії, hover-тінь/бордер за літеральними значеннями з макета. Кнопка «Додати в кошик»/«В кошику» — точний вигляд з макета (стан «в кошику» — без рамки/фону, іконка-галочка + текст + пігулка-лічильник, а не довільна кольорова пігулка, яка була раніше).
  - **Знайдений і виправлений реальний баг**: після переносу фільтра/пагінації в клієнтський компонент `getPublicUrl()` (читає `process.env.STORAGE_PUBLIC_BASE_URL` — доступний лише на сервері) опинився в клієнтському бандлі через `ProductCard`, який рендерився вже не з серверного, а з клієнтського компонента — усі фото на каталозі показували `undefined/products/...`. Виправлено за тим самим паттерном, що вже використовувався для Roboty/Novyny: URL фото резолвиться на сервері (`page.tsx`) і передається вже готовим рядком (`photoUrl`), а не сирим ключем (`mainPhoto`) — `ProductCard` більше не викликає `getPublicUrl()` сам.
- ~~Картка товару (`/shop/[slug]`)~~ — **готово, підтверджено Стасом 2026-09-25**. Виявилось суттєво складнішою за попередню версію:
  - **Галерея** — була «клік по мініатюрі міняє головне фото» (вигадана раніше, без опори на макет); в оригіналі головне фото статичне, під ним окрема стрічка (ковзне вікно 3/2 фото + стрілки + лайтбокс-модалка) — той самий підхід, що і в Roboty, свій розмір (480px головне фото, мініатюри 132×98, стрілки 42px).
  - **Опис товару** — у БД одне поле `description`, в макеті ж два рівні: короткий абзац завжди видно + кнопка «Детальніше про товар» розгортає решту. Узгоджено зі Стасом 2026-09-25: перший абзац (до першого порожнього рядка) — короткий, решта — під розгортанням; без змін у БД/адмінці. Кнопка «Поділитися» — за макетом лежить усередині цього розгортання (не окремо), `ShareButton` отримав `size="compact"` варіант під власні (менші) пікселі цього макета.
  - **Блок покупки** — степер максимум 10 (з `Math.min(10, qty+1)` у скрипті макета), і окрема мобільна розкладка `buywrap` (кількість+«Додати в кошик» в одному рядку, «Перейти до кошика» окремим рядком по центру) — цього не було зовсім.
  - **«Рекомендовані товари»** — виявилось, теж із пагінацією (по 3 на сторінку, стрілки+крапки+«Усі товари»), а не фіксовані 4 без пагінації, як було.
  - `ProductCard` отримав `photoHeight` проп (210px каталог / 190px рекомендовані — обидва значення реально різні в макетах, раніше скрізь стояло 210).
  - **Два баги, знайдені Стасом одразу після здачі й виправлені**: (1) лінки «Оплата й доставка»/«Умови повернення» в тексті доставки не мали кольору/hover — виявилось, у макеті це не властивість самих лінків, а ГЛОБАЛЬНЕ правило `a { color: #2B6BB8 } a:hover { color: #C9307C }` для всіх звичайних `<a>` в файлі, яке легко пропустити, якщо дивитись лише на конкретний елемент; (2) відступ до блоку «Рекомендовані товари» — я обгорнув його в окрему `<section>` зі своїм padding, через що відступи складались; в оригіналі це один і той самий `<section>`, що й для картки товару, з `margin-top:76px` на внутрішньому div — об'єднав в одну секцію.
- ~~Кошик (`/cart`)~~ — **готово, підтверджено Стасом 2026-09-25**. Була узагальнена центрована колонка без стилю; тепер точна структура з `design/VL - Cart (client).html`: та сама градієнтна шапка (ейбров «МАГАЗИН» + h1 «Кошик», лише ОДНА декоративна фігура — на відміну від Shop/Product тут нема другої "плаваючої" плями, перевірено — `vlFloat` в цьому файлі взагалі не використовується в розмітці), порожній кошик — окрема ілюстрація-заглушка з іконкою + кнопка «До каталогу» (раніше просто текст-посилання), сітка `1.65fr/1fr` — список товарів + липкий (`sticky top:110px`, тільки з lg) блок «Разом» справа. Рядок товару: фото-посилання, категорія над назвою, підписана «за одиницю» ціна, степпер 30×30, сума, кнопка видалення (**hover — червоний `#C0322C`, а не пурпуровий** — єдине місце в усьому магазині з іншим hover-кольором, точно з макета). Мобільна розкладка рядка — окрема 3-колонкова/2-рядкова сітка з кнопкою видалення, абсолютно позиціонованою в правий верхній кут — цього не було зовсім. `getCartProducts` (`src/server/actions/cart.ts`) тепер підтягує категорію товару (потрібна для бейджа в рядку, раніше не запитувалась).
- ~~Чекаут (`/checkout`, `/checkout/pay`, `/checkout/success`)~~ — **готово, підтверджено Стасом 2026-09-25**. Та сама структура з `design/VL - Checkout (client).html`: градієнтна шапка + сітка форма/сайдбар «Ваше замовлення», поля з фокус-обводкою magenta, Nova Poshta-поля перестильовані під точний вигляд макета (рамка magenta/45, dropdown) при збереженні живого пошуку через API (макет мав лише статичний список-заглушку — свідомо не копіювали цю частину дослівно). Кнопка оплати — «Оплатити {сума}», по-справжньому заблокована без згоди з умовами. Чекбокс згоди — 3 окремі лінки на `/legal/*` замість суцільного тексту. Сторінка успіху — картка з іконкою-галочкою за зразком модалки «Дякуємо!» з макета (в оригіналі це оверлей на тій самій сторінці, у нас — окремий `/checkout/success`, бо архітектура з реальним редіректом на WayForPay + вебхуком, узгоджена ще на самому початку роботи над магазином).
  - **Реальний баг, знайдений Стасом одразу після здачі**: чекбокс згоди фізично лежав поза `<form>` (лише кнопка сабміту мала `form="checkout-form"`) — його значення взагалі не потрапляло в дані форми, тому сервер завжди відмовляв з «потрібно погодитися», навіть при відміченій галочці. Виправлено додаванням `form="checkout-form"` на сам чекбокс.
  - **Той самий баг з лінками, що вже був на сторінці товару**: лінки в чекбоксі згоди мали колір navy замість blue/magenta hover — вирівняв під ту саму схему, що на сторінці товару (за окремим проханням Стаса, для візуальної єдності — в оригінальному макеті чекауту ці лінки взагалі мають колір `var(--navy)`, це свідоме відхилення від букви макета заради консистентності).
  - **Помилки полів не зникали при введенні** — той самий `dismissOnFill`-патерн, що вже використовується в адмінських формах, підключено і сюди (включно з Nova Poshta полями через нові колбеки `onCityInput`/`onWarehouseInput`).
  - **Автосабміт на `/checkout/pay` не спрацьовував** — `<script>`, вставлений через `dangerouslySetInnerHTML`, браузер не виконує (стандартна поведінка DOM для script-тегів, доданих через innerHTML) — клієнту доводилось тиснути кнопку вручну. Замінено на `AutoSubmitForm` (`useEffect` + `form.submit()`).
  - **Маска телефону**: як тільки з'являється перша цифра — одразу префікс `+380`, далі формат `XX-XXX-XX-XX`.
  - **Плавний скрол по якорях на Головній зник після фіксу скролу при переході між сторінками** — реальна причина: `ScrollResetOnNavigate` запам'ятовував "попереднє" значення `scrollBehavior`, а React StrictMode в dev-режимі подвійно викликає ефект (mount→cleanup→mount) — друге спрацювання запам'ятовувало вже зіпсоване "auto" з першого як "попереднє", назавжди залишаючи інлайновий стиль застряглим на "auto". Виправлено: завжди повертаємось до `""` (порожній рядок, тобто до plavного скролу з CSS), а не до запам'ятованого значення.
  - **Іконка кошика в хедері**: на сторінці `/cart` рамка й іконка стають magenta (`#C9307C`) замість звичайної navy/hover-indigo — точна деталь з макета `design/VL - Cart (client).html`, пропущена при першій верстці Header.
  - **Значки Visa/Mastercard** додані в ряд «Безпечна оплата карткою» на чекауті (файли з `assets/`, скопійовані в `public/`).
  - **Лого WayForPay у футері** (на всіх сторінках, окреме прохання Стаса — в макетах цього нема): десктоп — під іконками соцмереж, по нижньому краю вирівняне з «Умови повернення» (довелось перегрупувати сітку футера — легал-лінки й блок соцмережі+WayForPay розтягуються одне відносно одного окремо від блоку з лого/тегліном, інакше orientation "тягнеться" на найвищу колонку взагалі); мобільна — іконки соцмереж зліва, лого WayForPay справа в тому самому ряду. Збільшено з 32px до 48px за проханням Стаса («майже не видно»).
  - **WayForPay — платіжна інтеграція, знайдено й виправлено через research по офіційній документації та вихідниках сторонніх SDK** (публічна дока WayForPay сама по собі неповна):
    - Додано обов'язкове поле `merchantTransactionSecureType="AUTO"` — за їхньою докою обов'язкове для форми Purchase, у нас не передавалось зовсім.
    - Додано `orderLifetime` (3600с = 1 година) — обмежує час, протягом якого WayForPay прийме оплату по цьому замовленню; потрібно і саме по собі, і як гарантія безпеки для будь-якої майбутньої автоочистки на нашій стороні (щоб не виникло гонки станів "позначили Скасовано, а клієнт саме тоді таки оплатив").
    - Вебхук (`/api/payments/wayforpay/callback`) тепер обробляє не лише `Approved`, а й `Declined`/`Voided` → автоматично переводить замовлення в «Скасовано» (знайдено повний enum `transactionStatus` через вихідники стороннього TS-пакета — в офіційній доці WayForPay цього переліку нема).
    - **Важливе обмеження, з'ясоване в діалозі зі Стасом**: якщо клієнт натиснув «Оплатити», потрапив на сторінку WayForPay і просто закрив вкладку, нічого не вводячи — вебхука не буде взагалі (немає жодної спроби транзакції, яку WayForPay міг би відхилити). Це і привело до наступного пункту.
    - **Автоматична відмова "зависших" замовлень**: `PENDING_PAYMENT` старші за 2 години (свідомо вибрано з запасом в 1 годину понад `orderLifetime`, щоб виключити гонку станів) автоматично переводяться в «Скасовано» — перевіряється при кожному заході на сторінку списку замовлень (`expireStaleOrders()`, `src/lib/expire-stale-orders.ts`), без окремої crontab-інфраструктури.
    - **Спроба підставляти `clientEmail`/`clientPhone` у форму оплати (щоб не вводити повторно) — відкотено**: Стас помітив, що після цього на сторінці WayForPay зникали поля картки. Причина не встановлена напевно (ймовірно, щось у їхній системі розпізнавання "вже відомого" клієнта на спільному публічному демо-мерчанті `test_merch_n1`, яким користуються сторонні розробники) — вирішили не чіпати, повернути як було. Можна повернутися до цього при переході на бойовий мерчант.
    - **З'ясовано й повідомлено Стасу**: тестові картки `4111 1111 1111 1111`/`5454 5454 5454 5454` на демо-мерчанті `test_merch_n1` **завжди** повертають "Payment is unsuccessful" — це навмисна поведінка (тест сценарію відмови), а не помилка в нашому коді. Перевірити реальний успішний платіж наживо можна буде тільки на бойовому мерчанті.
- **Список замовлень в адмінці (`/admin/zamovlennia`) — перероблено за проханням Стаса** (поза чергою ревізії магазину, окремий запит):
  - Фільтри: прибрано кнопку «Застосувати» — фільтрація тепер миттєва (кожна зміна одразу оновлює URL через `router.replace`, без перезавантаження); «Скинути» → «Скинути усі фільтри», винесено з картки-підложки під неї; вирівняно висоту полів дат під висоту `CustomSelect` (підняли дати, а не зменшили селекти — той компонент спільний ще для форм товарів/новин, зменшувати його там, де не просили, не стали).
  - Список тепер таблицею (Дата замовлення / № замовлення / Покупець / Контакт / Сума / Статус) замість карток, липкий заголовок таблиці при скролі, пагінація по 10 (було 15) з вікном ±2 сторінки навколо поточної + перша/остання з "..." при великій кількості сторінок (раніше рендерились взагалі всі номери підряд без обмеження).
  - Клік по рядку — та сама картка замовлення, без змін (кожна комірка — окреме посилання-блок, вся строка клікабельна, справжня HTML-таблиця для доступності).
  - Кнопка «Експорт в Excel» (primary-стиль, зліва від «Скинути усі фільтри») — вивантажує CSV з тими самими фільтрами, що застосовані на екрані (UTF-8 з BOM, кирилиця відкривається в Excel коректно). Свідомо не ставили пакет `xlsx` — у нього відомі вразливості високої критичності в npm-версії (prototype pollution, ReDoS); CSV дає той самий результат без сторонньої залежності.
  - Тиха перевірка нових замовлень раз на 2 хвилини (`router.refresh()`, без повного перезавантаження) — за проханням Стаса, після того як з'ясувалось, що список не оновлюється сам собою, поки сторінка відкрита (звичайна поведінка SSR, не баг).
  - **Обговорено й свідомо не зроблено**: кнопка «Очистити кошик» на публічному сайті — в макеті її нема, для невеликого магазину видалення по одному вважається достатнім; масове видалення заявок, що зависли в очікуванні оплати — вирішили не видаляти фізично (контакти можуть знадобитись для маркетингу), обмежились автоматичною позначкою «Скасовано» через 2 години замість видалення.

**Юридичні сторінки** (`/legal/[slug]`, один динамічний роут на всі 4 документи) — **готово й підтверджено Стасом 2026-09-26**:
- Макет (`design/VL - Legal (client).html`) виявився прототипом лише ОДНОГО документа-прикладу («Оплата й доставка») з реальним текстом — це той самий шаблон, яким мали б рендеритись усі 4 документи в інструменті прототипування. Тексти решти трьох документів (Політика конфіденційності, Умови використання, Умови повернення) написані самостійно, з урахуванням вимог законодавства України (Закон «Про захист персональних даних» №2297-VI, Закон «Про захист прав споживачів»), включно з правилами обробки персональних даних у Політиці конфіденційності — за окремим проханням Стаса.
- Реквізити (Стас надав явно, не вигадані): ФОП Лемешко Вікторія Віталіївна, РНОКПП (ІПН) 2864116303, email `vik_vik@ukr.net`, телефон `+380 67 656 74 70`.
- Структура сторінки — точно з макета: sticky-сайдбар «Зміст» (якорі на розділи), блок «Питання щодо документа» → «Написати мені», стаття з h1/датою оновлення/розділами, і внизу — пігулки-посилання на інші 3 документи (звичайні `<Link>`, тому URL при переключенні реально змінюється, як і просив Стас). На адаптиві ці пігулки за проханням Стаса йдуть по одній зліва, кожна з нового рядка (`flex-col` на мобільному, `sm:flex-row sm:flex-wrap` на десктопі).
- **Перекладено на всі 3 мови сайту** (`LEGAL_DOCS`/`LEGAL_DOC_META` в `src/lib/legal-content.tsx` тепер `Record<AppLocale, ...>`) — за проханням Стаса, вже після того як українські тексти були узгоджені й відредаговані. Заголовки документів синхронізовано з уже існуючими лейблами футера (`Footer.privacyPolicy` тощо), бренд «ТМ «Єнот ЕМО»» — за вже усталеною на сайті конвенцією («TM «Enot EMO»» на EN, «ТМ «Енот ЭМО»» на RU). Службові підписи сторінки (Зміст/Contents/Содержание, Оновлено {date} тощо) винесені в новий namespace `Legal` у `messages/*.json`.
- **Правки контенту за прямими правками Стаса в процесі рев'ю**: у «4. Кому передаються дані» Політики конфіденційності прибрано зайву згадку «Укрпошти» (сайт інтегрований лише з Новою поштою); у «5. Контакти» «Оплата й доставка» прибрано посилання на «форму на головній сторінці» (лишився тільки email); в інтро цього ж документа додано «та продукції» («товарів магазину та продукції ТМ «Єнот ЕМО»» — тому що фактично це магазин товарів і продукції бренду, а не сам бренд).
- **Реальний баг, знайдений під час рев'ю (мобільна верстка)**: текст у списках (`<li>` з буллітом-кружком) обрізався замість переносу на вузьких екранах — класична flexbox-пастка: `<li>` є flex-контейнером, і його текстовий `<span>` без `min-w-0` не стискається нижче свого "мінімального контенту", через що рядок вилазив за межі viewport і `overflow-x: clip` на `body` його обрізав. Виправлено `min-w-0` на текстовому `<span>`.
- **Кнопка «Написати мені» відкриває ту саму модалку «Задати своє питання», що і в FAQ** (за проханням Стаса — раніше вела на форму на головній сторінці):
  - Перша спроба (передати функцію-тригер пропсом із серверного `page.tsx` прямо в клієнтський `AskQuestionModal`) впала з 500-ю помилкою — Next.js не серіалізує функції через межу Server/Client Component. Виправлено через окремий маленький клієнтський компонент-обгортку (`src/components/legal/write-to-me-trigger.tsx`), який сам створює тригер і монтує ту саму модалку.
  - **Другий реальний баг, знайдений Стасом одразу після здачі**: модалка не перекривала хедер сайту (він лишався "поверх", не затемненим) — причина в тому, що `<aside>` із `sticky`-позиціюванням у сайдбарі документа завжди створює власний stacking context у браузері (це стосується `position: sticky`/`fixed` завжди, незалежно від того, чи заданий `z-index`), і z-index оверлея модалки "застрягав" усередині нього, не пробиваючись вище хедера. Виправлено через `createPortal` у `document.body` — тепер оверлей завжди монтується напряму в body, незалежно від того, звідки викликаний тригер.
  - **Третій момент, підмічений самим Стасом**: якщо натиснути на «Політикою конфіденційності» всередині цієї-таки модалки (у дисклеймері про згоду на обробку даних), модалка мала б закритись, а не лишатись висіти поверх сторінки — особливо безглуздо, якщо користувач і так вже на `/legal/pryvatnist`. Додано `onClose` колбек, що передається в `AskForm` і викликається на `onClick` цього лінка.
- **Дисклеймер «Надсилаючи форму, ви погоджуєтесь з Політикою конфіденційності»** (уже існував на формі запису на консультацію, доданий за аналогією і в модалку FAQ) — за проханням Стаса: колір лінка вирівняно під checkout (`text-blue hover:text-magenta`), прибрано крапку в кінці фрази й «Політикою конфіденційності»/«Privacy Policy»/«Политикой конфиденциальности» тепер з великої літери — в усіх трьох мовах.
- **Компактність модалки «Задати своє питання»** (за проханням Стаса, щоб вміст не скролився): зменшено textarea (4→3 рядки), відступи між полями (`gap-4`→`gap-3`) і паддинги модалки. Перевірено headless-браузером на екрані 390×700 — вміст влазить точно, без скролу.
- **Реальний сайтовий баг, знайдений і виправлений під час цієї роботи (не специфічний для Legal, стосується будь-якої сторінки з перемикачем мови)**: перемикання мови скидало скрол сторінки нагору, хоча в `router.replace(pathname, {locale, scroll:false})` це явно вимкнено. Справжня причина — зміна параметра `[locale]` призводить до повного перемонтування кореневого `layout.tsx` (включно з `ScrollResetOnNavigate`), тому компонент запускається так, ніби це звичайний перехід на нову сторінку. Виправлено через `sessionStorage`: `preserveScrollForLocaleSwitch()` викликається в хедері безпосередньо перед `router.replace`, зберігаючи поточний `scrollY`; компонент після перемонтування читає й відновлює саме це значення (замість скидання на 0) і видаляє ключ лише один раз (ідемпотентно відносно React StrictMode double-invoke в dev). Перевірено: звичайна навігація між сторінками, як і раніше, скидає скрол нагору — регресії нема.

## Ідеї на майбутнє (не зараз, свідомо відкладено)

- **Кроп заголовного фото на «Мої роботи» (детальна сторінка + адмінка)**: фото-рамка має фіксовану пропорцію (в макеті буквально `height: 440px`, ширина — вся колонка статті) й заповнюється через `object-cover` — тобто завжди обрізає фото по центру під цю рамку, а не показує його цілком; на мобільному рамка іншої ширини (на весь екран), тому й кроп виглядає інакше, ніж на десктопі. Це поведінка макета, не баг. Стас запитав про це 2026-09-25, вирішили поки не чіпати. Можливі варіанти на майбутнє, якщо знадобиться: (1) нічого не міняти в коді, попросити Вікторію заздалегідь кадрувати фото під потрібні пропорції; (2) додати в адмінці вибір точки фокусу кропу (верх/центр/низ), щоб важлива частина фото не обрізалась; (3) `object-contain` замість `object-cover` — фото повністю видно, але з'являються порожні поля, якщо пропорції не збігаються (відхід від чистого вигляду в макеті).
- **Автоматичне створення ТТН через API Нової пошти**: зараз (і за узгодженим планом навіть після підключення ключа НП) Вікторія вручну створює накладну у своєму кабінеті НП і вручну тисне «Позначити відправленим» в адмінці (architecture.md, розд. 5.4). Повна інтеграція (автостворення ТТН, автоматичний перехід у SHIPPED) — окрема, значно більша задача; свідомо не робимо зараз.
- **Накладений платіж (оплата при отриманні)**: зараз магазин рахує лише повну передоплату, і статусна модель Order (`PENDING_PAYMENT → PAID → SHIPPED → CANCELLED`) на це й розрахована (відправка можлива тільки зі статусу PAID). Якщо пізніше з'явиться накладений платіж, знадобиться або окреме поле способу оплати, або додатковий статус (типу "Відправлено, очікує оплату при отриманні") — поточний enum для такого сценарію не підходить. Обговорювали зі Стасом 2026-09-24, вирішили не закладати наперед.
